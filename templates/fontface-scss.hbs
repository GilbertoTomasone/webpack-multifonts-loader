{{#each fonts}}
@font-face {
	font-family: "{{fontName}}";
	src: {{{src}}};
}
{{/each}}

@mixin {{mixinName}}($name, $weight: normal, $style: normal) {
	font-family: $name;
	font-weight: unquote($weight);
	font-style: unquote($style);
}